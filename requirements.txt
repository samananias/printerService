# Web layer (README Section 4)
fastapi>=0.110
uvicorn[standard]>=0.29

# Required for file uploads — FastAPI needs it to parse multipart/form-data
python-multipart>=0.0.9

# Windows printing API (README Section 4) — Windows only
pywin32>=306; sys_platform == "win32"

# Image processing (Phase 2, multi-format plan): converts JPG/PNG/WebP into
# print-ready PDF pages. 10.3+ has the image-parsing security fixes.
pillow>=10.3
