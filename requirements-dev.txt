# Development-only tools — the service itself never needs these (that's
# why they live separately from requirements.txt). CI installs both files.
#
#   pytest       the test runner (tests/)
#   pytest-cov   measures which lines the tests actually executed (coverage)
#   httpx        required by FastAPI's TestClient to fake HTTP requests
#   ruff         the linter: flags real bugs + style drift, without reformatting

pytest>=8
pytest-cov>=5
httpx>=0.27
ruff>=0.6
